<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender: Logs to the console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender: Logs to a file (optional) -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file> <!-- Logs will be written to this file -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Log level configuration for specific packages -->
    <logger name="org.springframework" level="INFO" />
    <logger name="com.ThintureGpsTracker" level="DEBUG" />

    <!-- Root logger configuration -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" /> <!-- Optional: Add this if you want to log to a file -->
    </root>

</configuration>
